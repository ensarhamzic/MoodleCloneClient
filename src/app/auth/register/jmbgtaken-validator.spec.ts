import { JMBGTakenValidator } from './jmbgtaken-validator';

describe('JMBGTakenValidator', () => {
  it('should create an instance', () => {
    expect(new JMBGTakenValidator()).toBeTruthy();
  });
});
