import { UsernameTakenValidator } from './username-taken-validator';

describe('UsernameTakenValidator', () => {
  it('should create an instance', () => {
    expect(new UsernameTakenValidator()).toBeTruthy();
  });
});
