import { EmailTakenValidator } from './email-taken-validator';

describe('EmailTakenValidator', () => {
  it('should create an instance', () => {
    expect(new EmailTakenValidator()).toBeTruthy();
  });
});
